function takeANumber(lineArray, newPerson){
  lineArray.push(newPerson);
  var message = `Welcome, ${name}. You are number ${katzDeli.length} in line.`
  console.log(message);
  return message;
}

"Welcome, " + newPerson + ". You are number " + lineArray.length + " in line."